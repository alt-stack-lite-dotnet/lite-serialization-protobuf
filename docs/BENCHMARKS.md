# Benchmarks (repo)

Benchmarks live in `benchmark/` and are **not** part of the shipped package.

Run:

```bash
dotnet run -c Release --project benchmark/Lite.Serialization.Protobuf.Benchmarks/Lite.Serialization.Protobuf.Benchmarks.csproj
```

